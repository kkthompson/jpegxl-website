{
  "name": "jpegxl-website",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:distance-data": "node scripts/generate-distance-data.mjs",
    "predev": "npm run generate:distance-data",
    "dev": "astro dev",
    "prebuild": "npm run generate:distance-data",
    "build": "ASTRO_TELEMETRY_DISABLED=1 astro build",
    "preview": "ASTRO_TELEMETRY_DISABLED=1 astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.7",
    "marked": "^18.0.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "prettier": "^3.8.3",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^5.0.0"
  }
}
